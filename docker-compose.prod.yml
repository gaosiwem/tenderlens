services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    environment:
      - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL:?LETSENCRYPT_EMAIL is required}
      - MARKETING_DOMAIN=${MARKETING_DOMAIN:?MARKETING_DOMAIN is required}
      - APP_DOMAIN=${APP_DOMAIN:?APP_DOMAIN is required}
      - API_DOMAIN=${API_DOMAIN:?API_DOMAIN is required}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend
      - backend

  redis:
    image: redis:7
    command: ["redis-server", "--appendonly", "yes"]
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER:?POSTGRES_USER is required}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - POSTGRES_DB=${POSTGRES_DB:-tenderlens_db}
    volumes:
      - supabase_db_data:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:?POSTGRES_USER is required} -d ${POSTGRES_DB:-tenderlens_db}"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=8080
      - DATABASE_URL=${DATABASE_URL:?DATABASE_URL is required}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET:?JWT_ACCESS_SECRET is required}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:?JWT_REFRESH_SECRET is required}
      - COOKIE_SECRET=${COOKIE_SECRET:?COOKIE_SECRET is required}
      - COOKIE_SECURE=${COOKIE_SECURE:-true}
      - COOKIE_DOMAIN=${COOKIE_DOMAIN:-}
      - FRONTEND_URL=${FRONTEND_URL:?FRONTEND_URL is required}
      - CORS_ORIGINS=${CORS_ORIGINS:?CORS_ORIGINS is required}
      - STORAGE_DRIVER=${STORAGE_DRIVER:-supabase}
      - SUPABASE_S3_ENDPOINT=${SUPABASE_S3_ENDPOINT:-}
      - SUPABASE_S3_BUCKET=${SUPABASE_S3_BUCKET:-}
      - SUPABASE_S3_ACCESS_KEY_ID=${SUPABASE_S3_ACCESS_KEY_ID:-}
      - SUPABASE_S3_SECRET_ACCESS_KEY=${SUPABASE_S3_SECRET_ACCESS_KEY:-}
      - SUPABASE_S3_REGION=${SUPABASE_S3_REGION:-auto}
      - SUPABASE_S3_FORCE_PATH_STYLE=${SUPABASE_S3_FORCE_PATH_STYLE:-true}
      - SMTP_HOST=${SMTP_HOST:-}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER:-}
      - SMTP_PASS=${SMTP_PASS:-}
      - PAYFAST_MERCHANT_ID=${PAYFAST_MERCHANT_ID:-}
      - PAYFAST_MERCHANT_KEY=${PAYFAST_MERCHANT_KEY:-}
      - PAYFAST_PASSPHRASE=${PAYFAST_PASSPHRASE:-}
      - PAYFAST_SANDBOX=${PAYFAST_SANDBOX:-true}
      - PAYFAST_RECONCILIATION_ENABLED=${PAYFAST_RECONCILIATION_ENABLED:-true}
      - "PAYFAST_RECONCILIATION_CRON=${PAYFAST_RECONCILIATION_CRON:-15 * * * *}"
      - PAYFAST_GRACE_DAYS=${PAYFAST_GRACE_DAYS:-7}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
      - SMS_ENABLED=${SMS_ENABLED:-false}
      - SMS_PROVIDER=${SMS_PROVIDER:-smtp2go}
      - SMS_FROM_NUMBER=${SMS_FROM_NUMBER:-}
      - SMTP2GO_SMS_API_URL=${SMTP2GO_SMS_API_URL:-https://api.smtp2go.com/v3/sms/send}
      - SMTP2GO_API_KEY=${SMTP2GO_API_KEY:-}
      - SMTP2GO_SMS_SENDER=${SMTP2GO_SMS_SENDER:-}
      - SMSSOUTHAFRICA_API_URL=${SMSSOUTHAFRICA_API_URL:-https://rest.mymobileapi.com/v3/BulkMessages}
      - SMSSOUTHAFRICA_CLIENT_ID=${SMSSOUTHAFRICA_CLIENT_ID:-}
      - SMSSOUTHAFRICA_CLIENT_SECRET=${SMSSOUTHAFRICA_CLIENT_SECRET:-}
      - SMSSOUTHAFRICA_SENDER_ID=${SMSSOUTHAFRICA_SENDER_ID:-}
      - SMSSOUTHAFRICA_TEST_MODE=${SMSSOUTHAFRICA_TEST_MODE:-false}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - SENTRY_DSN=${SENTRY_DSN:-}
      - COMPLIANCE_AUDITOR_ENABLED=${COMPLIANCE_AUDITOR_ENABLED:-true}
      - COMPLIANCE_AUDITOR_MODEL=${COMPLIANCE_AUDITOR_MODEL:-gpt-4o-mini}
      - COMPLIANCE_AUDITOR_MAX_TOKENS=${COMPLIANCE_AUDITOR_MAX_TOKENS:-1800}
      - COMPLIANCE_AUDITOR_MAX_CHUNKS=${COMPLIANCE_AUDITOR_MAX_CHUNKS:-40}
      - COMPLIANCE_AUDITOR_QUEUE_ENABLED=${COMPLIANCE_AUDITOR_QUEUE_ENABLED:-true}
      - COMPLIANCE_AUDITOR_SCORE_PASS=${COMPLIANCE_AUDITOR_SCORE_PASS:-80}
      - COMPLIANCE_AUDITOR_SCORE_WARNING=${COMPLIANCE_AUDITOR_SCORE_WARNING:-60}
      - BID_REVIEWER_ENABLED=${BID_REVIEWER_ENABLED:-true}
      - BID_REVIEWER_MODEL=${BID_REVIEWER_MODEL:-gpt-4o-mini}
      - BID_REVIEWER_MAX_TOKENS=${BID_REVIEWER_MAX_TOKENS:-2200}
      - BID_REVIEWER_MAX_TENDER_CHUNKS=${BID_REVIEWER_MAX_TENDER_CHUNKS:-30}
      - BID_REVIEWER_MAX_PROPOSAL_CHUNKS=${BID_REVIEWER_MAX_PROPOSAL_CHUNKS:-50}
      - BID_REVIEWER_QUEUE_ENABLED=${BID_REVIEWER_QUEUE_ENABLED:-true}
      - BID_REVIEWER_SCORE_PASS=${BID_REVIEWER_SCORE_PASS:-80}
      - BID_REVIEWER_SCORE_WARNING=${BID_REVIEWER_SCORE_WARNING:-60}
      - BACKEND_PUBLIC_URL=${BACKEND_PUBLIC_URL:?BACKEND_PUBLIC_URL is required}
    volumes:
      - tenderlens_storage:/app/storage
    expose:
      - "8080"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: ["npm", "run", "worker:start"]
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL:?DATABASE_URL is required}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET:?JWT_ACCESS_SECRET is required}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:?JWT_REFRESH_SECRET is required}
      - COOKIE_SECRET=${COOKIE_SECRET:?COOKIE_SECRET is required}
      - STORAGE_DRIVER=${STORAGE_DRIVER:-supabase}
      - SUPABASE_S3_ENDPOINT=${SUPABASE_S3_ENDPOINT:-}
      - SUPABASE_S3_BUCKET=${SUPABASE_S3_BUCKET:-}
      - SUPABASE_S3_ACCESS_KEY_ID=${SUPABASE_S3_ACCESS_KEY_ID:-}
      - SUPABASE_S3_SECRET_ACCESS_KEY=${SUPABASE_S3_SECRET_ACCESS_KEY:-}
      - SUPABASE_S3_REGION=${SUPABASE_S3_REGION:-auto}
      - SUPABASE_S3_FORCE_PATH_STYLE=${SUPABASE_S3_FORCE_PATH_STYLE:-true}
      - PAYFAST_MERCHANT_ID=${PAYFAST_MERCHANT_ID:-}
      - PAYFAST_MERCHANT_KEY=${PAYFAST_MERCHANT_KEY:-}
      - PAYFAST_PASSPHRASE=${PAYFAST_PASSPHRASE:-}
      - PAYFAST_SANDBOX=${PAYFAST_SANDBOX:-true}
      - PAYFAST_RECONCILIATION_ENABLED=${PAYFAST_RECONCILIATION_ENABLED:-true}
      - "PAYFAST_RECONCILIATION_CRON=${PAYFAST_RECONCILIATION_CRON:-15 * * * *}"
      - PAYFAST_GRACE_DAYS=${PAYFAST_GRACE_DAYS:-7}
      - SMS_ENABLED=${SMS_ENABLED:-false}
      - SMS_PROVIDER=${SMS_PROVIDER:-smtp2go}
      - SMS_FROM_NUMBER=${SMS_FROM_NUMBER:-}
      - SMTP2GO_SMS_API_URL=${SMTP2GO_SMS_API_URL:-https://api.smtp2go.com/v3/sms/send}
      - SMTP2GO_API_KEY=${SMTP2GO_API_KEY:-}
      - SMTP2GO_SMS_SENDER=${SMTP2GO_SMS_SENDER:-}
      - SMSSOUTHAFRICA_API_URL=${SMSSOUTHAFRICA_API_URL:-https://rest.mymobileapi.com/v3/BulkMessages}
      - SMSSOUTHAFRICA_CLIENT_ID=${SMSSOUTHAFRICA_CLIENT_ID:-}
      - SMSSOUTHAFRICA_CLIENT_SECRET=${SMSSOUTHAFRICA_CLIENT_SECRET:-}
      - SMSSOUTHAFRICA_SENDER_ID=${SMSSOUTHAFRICA_SENDER_ID:-}
      - SMSSOUTHAFRICA_TEST_MODE=${SMSSOUTHAFRICA_TEST_MODE:-false}
      - SENTRY_DSN=${SENTRY_DSN:-}
      - COMPLIANCE_AUDITOR_ENABLED=${COMPLIANCE_AUDITOR_ENABLED:-true}
      - COMPLIANCE_AUDITOR_MODEL=${COMPLIANCE_AUDITOR_MODEL:-gpt-4o-mini}
      - COMPLIANCE_AUDITOR_MAX_TOKENS=${COMPLIANCE_AUDITOR_MAX_TOKENS:-1800}
      - COMPLIANCE_AUDITOR_MAX_CHUNKS=${COMPLIANCE_AUDITOR_MAX_CHUNKS:-40}
      - COMPLIANCE_AUDITOR_QUEUE_ENABLED=${COMPLIANCE_AUDITOR_QUEUE_ENABLED:-true}
      - COMPLIANCE_AUDITOR_SCORE_PASS=${COMPLIANCE_AUDITOR_SCORE_PASS:-80}
      - COMPLIANCE_AUDITOR_SCORE_WARNING=${COMPLIANCE_AUDITOR_SCORE_WARNING:-60}
      - BID_REVIEWER_ENABLED=${BID_REVIEWER_ENABLED:-true}
      - BID_REVIEWER_MODEL=${BID_REVIEWER_MODEL:-gpt-4o-mini}
      - BID_REVIEWER_MAX_TOKENS=${BID_REVIEWER_MAX_TOKENS:-2200}
      - BID_REVIEWER_MAX_TENDER_CHUNKS=${BID_REVIEWER_MAX_TENDER_CHUNKS:-30}
      - BID_REVIEWER_MAX_PROPOSAL_CHUNKS=${BID_REVIEWER_MAX_PROPOSAL_CHUNKS:-50}
      - BID_REVIEWER_QUEUE_ENABLED=${BID_REVIEWER_QUEUE_ENABLED:-true}
      - BID_REVIEWER_SCORE_PASS=${BID_REVIEWER_SCORE_PASS:-80}
      - BID_REVIEWER_SCORE_WARNING=${BID_REVIEWER_SCORE_WARNING:-60}
    volumes:
      - tenderlens_storage:/app/storage
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:?NEXT_PUBLIC_API_BASE_URL is required}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:?NEXT_PUBLIC_API_URL is required}
        NEXT_PUBLIC_MARKETING_URL: ${NEXT_PUBLIC_MARKETING_URL:?NEXT_PUBLIC_MARKETING_URL is required}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:?NEXT_PUBLIC_APP_URL is required}
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID:-}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID:-}
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:?NEXT_PUBLIC_API_BASE_URL is required}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:?NEXT_PUBLIC_API_URL is required}
      - NEXT_PUBLIC_MARKETING_URL=${NEXT_PUBLIC_MARKETING_URL:?NEXT_PUBLIC_MARKETING_URL is required}
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:?NEXT_PUBLIC_APP_URL is required}
      - NEXT_PUBLIC_GOOGLE_CLIENT_ID=${NEXT_PUBLIC_GOOGLE_CLIENT_ID:-}
      - NEXT_PUBLIC_GA_MEASUREMENT_ID=${NEXT_PUBLIC_GA_MEASUREMENT_ID:-}
      - NEXT_PUBLIC_SENTRY_DSN=${NEXT_PUBLIC_SENTRY_DSN:-}
      - NEXT_PUBLIC_SENTRY_ENVIRONMENT=${NEXT_PUBLIC_SENTRY_ENVIRONMENT:-production}
      - NEXT_PUBLIC_SENTRY_RELEASE=${NEXT_PUBLIC_SENTRY_RELEASE:-}
    expose:
      - "3005"
    depends_on:
      - backend

volumes:
  tenderlens_storage:
  redis_data:
  supabase_db_data:
  caddy_data:
  caddy_config:
